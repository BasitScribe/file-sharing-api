# File Sharing API

Express.js backend for a mini file sharing system (Cloudinary + MongoDB).

## Quick start (local, docker-compose)

1. Copy `.env.example` to `.env.docker` and fill values (for docker-dev)
2. Run:
   ```bash
   docker-compose -f docker-compose.dev.yml up --build
